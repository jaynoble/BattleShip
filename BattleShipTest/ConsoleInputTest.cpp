#include <gtest\gtest.h>

// should ask for fire coordinates
// shouldDisplayHelp...
// shouldExitOnExit

TEST(InputTest, should)
{
    // Instantiate UserIOInterface(myFakeStream)
    // for each choice in enum
    //   ASSERT_EQ(enum, userIO.getChoice)
    // invalid choice relaunches getChoice
}